{
  "_comment": "Generic MCP stdio server config. Adapt to your specific MCP client's config format.",
  "_note": "Replace /ABSOLUTE/PATH/TO/uv with the output of: which uv",
  "mcpServers": {
    "mempalace": {
      "command": "/ABSOLUTE/PATH/TO/uv",
      "args": ["run", "python", "-m", "mempalace.mcp_server"],
      "env": {}
    }
  }
}
