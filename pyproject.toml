[project]
name = "mempalace-mcp-bridge"
version = "0.1.0"
description = "MemPalace MCP bridge for devcontainer integration"
requires-python = ">=3.12"
dependencies = [
    "chromadb<0.7",
    "mempalace>=3.0.0",
]
